
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <link rel="stylesheet" href="./css/style.css">
  <link rel="stylesheet" href="MentoProject/assets/css/scrollbar.css" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css" integrity="sha512-KfkfwYDsLkIlwQp6LFnl8zNdLGxu9YAA1QvwINks4PhcElQSvqcyVLLD9aMhXd13uQjoXtEKNosOWaZqXgel0g==" crossorigin="anonymous" referrerpolicy="no-referrer" />
  <title>Sign up | Mento - Share your project with the world</title>
</head>
<body>
  <div class="split-screen">
    <div class="left">
    <video id="background-video" autoplay loop muted >
  <source src="./assets/videos/video.mp4" type="video/mp4">
</video>
        <section class="copy">
          <h1>Explore Amazing projects</h1>
          <p>Share your work with the wolrd and get paid. </p>

        </section>
    </div>
    <div class="right" >
         <form action="/register"   method="post">
         <div class="main-mockup">
          {{#if err}}
            <h1 style="font-size:1.5rem;background:rgba(235, 17, 17, 0.865);width:100%;color:white;font-family:'Courier New', Courier, monospace;text-align:center;padding:0.5em 0;border-radius:0.2em;">{{msg}}</h1>
          {{/if}}
           </div>
           <section class="copy">
             <h2>Sign Up</h2>
           
           </section>
           <div class="input-container name">
              <label for="fname">Full name</label>
              <input type="text" id="fname" class="name" name="name" placeholder="Enter Your Name"  required>
              <h6 id="fname-err" style="color:red;margin:-1em auto;text-align: center;font-family: 'Courier New', Courier, monospace;"></h6>
           </div>

           <div class="input-container email">
              <label for="email">Email</label>
              <input type="email" id="email" class="email" name="email" placeholder="Enter Your Email" required>
           </div>
           <div class="input-container password">
              <label for="password">Password</label>
              <input type="password" id="password" class="password" name="password" placeholder="Enter Your Password" required>
              <h6 id="pass-err" style="color:red;margin:-1em auto 0.5em auto;text-align: center;font-family: 'Courier New', Courier, monospace;"></h6>
           </div>
           <div class="input-container password">
              <label for="Cpassword">Confirm Password</label>
              <input type="password" id="Cpassword" class="confirm-password" name="confirm-password" placeholder="Enter Your Confirm Password" required>
              <h6 id="c-pass-err" style="color:red;margin:-1em auto;text-align: center;font-family: 'Courier New', Courier, monospace;"></h6>
           </div>
           <div class="input-container cta">
          
           </div>
           <div class="login-container">
               <p>Already have an account ? <a href="login">
                 <strong >Log In</strong>
               </a></p>
             </div>
           <button class="signup-btn" id="signup-btn" type="submit" name="submit" disabled>
            Sign up
        </button>
            
            <section class="copy legal"  style="text-align:center">
              <p><span class="small">By continuing, you agree to accept our <br><a href="#">Privacy Policy</a> &amp; <a href="#">Terms of Service</a></span></p>
            </section>
         </form>
    </div>
  </div>
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
    <script>
        $(document).ready(function (c) {
            $('.alert-close').on('click', function (c) {
                $('.main-mockup').fadeOut('slow', function (c) {
                    $('.main-mockup').remove();
                });
            });
        });
      $('#password').keyup(e=>{
        disableBtn()
        if($('#password').val().length < 8){
          $('#pass-err').text('Password should be longer than 8 charecters')
        }
        else{
          $('#pass-err').text('')
        }
      })
      $('#Cpassword').keyup(e=>{
        disableBtn()
        if($('#password').val() == $('#Cpassword').val()){
          $('#c-pass-err').text('')
        }
        else{
          $('#c-pass-err').text('Confirm Password and Passwords doesnt match')
        }
      })
      $('#fname').keyup(e=>{
        if($('#fname').val().length < 5)
          $('#fname-err').text('Username too small')
        else if($('#fname').val().length > 20)
          $('#fname-err').text('Username too big')
        else
          $('#fname-err').text('')
      })
      function disableBtn(){
        if($('#password').val().length < 8)
          $('#signup-btn').prop('disabled',true)
        else
          $('#signup-btn').prop('disabled',false)
        if($('#password').val() == $('#Cpassword').val() && $('#password').val().length != 0)
          $('#signup-btn').prop('disabled',false)
        else
          $('#signup-btn').prop('disabled',true)
        if($('#fname').val() < 5)
          $('#signup-btn').prop('disabled',true)
        else if($('#fname').val() < 5)
          $('#signup-btn').prop('disabled',true)
        else
          $('#signup-btn').prop('disabled',false)
      }
    </script>

</body>
<style>
  .signup-btn:disabled{
    opacity: 0.5;
    cursor: not-allowed;
  }
  .signup-btn:disabled:hover{
    opacity: 0.5;
    cursor: not-allowed;
    background-color: #00BA7C;
  }
</style>
</html>